# RETIRED: this filename is retained only to make old operator notes fail safe.
#
# Do not add services here. Use:
#   - deploy/docker-compose.existing-stack.yml for the marketing agent
#   - deploy/n8n/core-stack.release1-postgres.yml for the first n8n migration
#   - deploy/n8n/core-stack.release2-queue.yml only after release 1 is stable
#
# `docker compose -f deploy/docker-compose.core.yml up` intentionally starts
# nothing instead of recreating the former public databases and second n8n.
name: wamocon-retired-core-compose
services: {}
